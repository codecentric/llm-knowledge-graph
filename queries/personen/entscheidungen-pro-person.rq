# Welche Person hat welche Entscheidung getroffen?
#
# Ausführen:
#   node /workspace/.agents/skills/sparql-query/scripts/query.js \
#     --file /workspace/graph/personen/thomas.ttl \
#     --file /workspace/graph/personen/sarah.ttl \
#     --file /workspace/graph/personen/julia.ttl \
#     --file /workspace/graph/personen/lena.ttl \
#     --file /workspace/graph/personen/marco.ttl \
#     --query /workspace/queries/personen/entscheidungen-pro-person.rq

PREFIX foaf:   <http://xmlns.com/foaf/0.1/>
PREFIX person: <https://shop.example.org/personen#>
PREFIX rdfs:   <http://www.w3.org/2000/01/rdf-schema#>
PREFIX dct:    <http://purl.org/dc/terms/>

SELECT ?personLabel ?entscheidungLabel ?datum ?beschreibung
WHERE {
  ?p a foaf:Person ;
     rdfs:label ?personLabel ;
     person:hatEntschieden ?entscheidung .
  ?entscheidung rdfs:label ?entscheidungLabel .
  OPTIONAL { ?entscheidung dct:date ?datum . }
  OPTIONAL { ?entscheidung dct:description ?beschreibung . }
}
ORDER BY ?personLabel ?datum
